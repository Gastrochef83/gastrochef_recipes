-- supabase/migrations/001_init.sql
-- NOTE:
-- This repository previously had an incorrect file here (TSX code inside a .sql migration).
-- That can break Supabase migration tooling.
--
-- ✅ This placeholder keeps migrations valid.
-- If you need a real init schema, create a new migration like:
--   supabase migration new init_schema
-- and paste your schema SQL there.

BEGIN;

-- (intentionally empty)

COMMIT;
