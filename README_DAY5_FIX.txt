DAY 5 FIX (Build)
- Restores missing export convertQtyToPackUnit in src/core/recipeEngine/units.ts.
- Keeps grams/kg helpers + adds scaling.ts (servings scaling).

Apply by replacing ONLY the files in this ZIP (drop into repo root).
